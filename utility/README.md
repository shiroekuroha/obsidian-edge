# .vscode
## This is for VSCode users, other IDEs configs will be added after v1.0.0
You can copy this folder into project dir to force vscode to use clangd.

# scripts
## Do not use these scripts in it's directory
Copy `build_cached.sh` and `build_fresh.sh` to project dir to use it.
